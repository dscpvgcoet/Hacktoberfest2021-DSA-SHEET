#Bubble Sort

arr =[6,30,17,12,23]

for i in range(1,len(arr)-1):
    for j in range(len(arr)-1):
        if arr[j+1]<arr[j]:
            temp = arr[j+1]
            arr[j+1] = arr[j]
            arr[j] = temp

print(arr)
